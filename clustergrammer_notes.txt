grammer 7-14-2015
	I'm working on an app that allows a user to input a tsv separated matrix and obtain a clutergram visualization. I have a few bugs that need to be fixed so that the clustergram will work for small matrices. 

	Here's how to get into debug mode in javascript: 
  // debugger
  debugger;

To Do
	*. fix overflow on the left bug
	*. fix thin tiles bug
	4. determine if network formatting is incorreact and alert user rather than failing
	
Low priority 
	3. fix overflow from left side of column labels bug
	5. set color of the clustergram based on matrix data, black for positive data only, red/blue for up/dn data. 
	6. allow uploading excel files using python's excel uploading capabilities 

grammer 7-15-2015  

	Avi recommended changes
		1. Suggests to rename it Clustogrammer instead of grammer
		2. Add options
			a. colors
			b. cluster by rows or columns only
			c. specify distance metric
			d. specify linkage type
		3. Allow users to manually swap the orders of the rows and columns
		4. Provide save functionality to SVG, EPS, PNG, PDF
		5. PRovide APIs that will submit the matrix to you as a JSON and they you will fire the app in a new winow or embedded in an existing page. 

	I'm going to implement the following changes, which are a combination of my own and Avi's changes. 

	To Do
		1. save data to mongodb, provide permanent link to user 
		1.1 provide export image capability, SVG, EPS, PNG, PDF
		2. add user login 
		3. add clustering/viz options (manual reordering)
		4. Provide API that GEO2Enrichr can comunitate with 
		5. add note taking capabilities 
		6. d3 viz changes
			1. add general ability to have bars on col and row titles 
			2. add dendrogram-like view made from colored rects 
			3. fix zoom reset col/row label reordering bug
			4. clean up variable height of tiles
			5. fix narrow tile bug 
			6. improve row/column highlighting search 
			7. set up optional highlighting of tiles if there is evidence added
			8. automatically identify clusters and control cluster sizes using variable incosistency 
			9. have the numeric value appear on hover over a tile
			10. clean up the css, there are a huge number of unnecessary components in the custom css 
			11. add initial ordering as an optional argument, default to 'clust'
			12. improve onclick callback function - prevent click when dragging and double clicking 
			13. get canvas version of the code working (will consist of one svg and one canvas)
			14. set up variable color triangles 
			15. allow ordering by user-defined categories 
			16. allow more than one user-defined categories with user-defined category-names and colors 
			17. keep the horizontal white lines in place during the transition - only move the tiles 
			18. allow manual switching of rows and columns (its not clear how to allow the user to control this)
			19. column search highlighting only (no zooming)
			-. allow column search (this is not a feature I want right now, I will limit column search to highlighting)
			- user defined categories can be defined in the row/col nodes or in the input matrix 
			-. fix hover highlighting bug - it appears to only hover when its over the border lines 
			-. set sizing to outside div
			-. clean up column label spillover on left side
			-. add optional callback function for clicking on tile 
			-. fix scrolling bug, the entire page scrolls slightly to the left or right when panning the clustergram
			-. allow fixed or variable clustergram size, default to varaible with required outer margins 
			-. clean up how the matrix is made in d3_clustergram.js 
			-. have variable domain/range cutoff controls, have it default to the max up/dn from the data 
			-. add row and col labels into svg and have inputs for these
			-. add row labeling triangles 
			-. remove global variables 


mongodb
	Qiaonan will send me the ip address for mongodb, the one hosted on one of our servers. I can then make a database within the mongodb, use robmongo for something(?) and then write to the database using Pymongo. 

code to make triangles
// // add triangle under rotated labels
  // d3.select(this)
  //   .selectAll('path')
  //   .attr('d', function(d) { 
  //     origin_x = 0;
  //     var origin_y = params.x_scale.rangeBand()/16.66;
  //     var mid_x  = -(params.x_scale.rangeBand()/2) / zoom_y;
  //     var mid_y  =  (params.x_scale.rangeBand()/2)/params.zoom_switch ;
  //     var final_x  =  0;
  //     var final_y  =  params.x_scale.rangeBand()/params.zoom_switch - params.x_scale.rangeBand()/16.66  ;
  //     var output_string = 'M '+origin_x+','+origin_y+' L ' + mid_x + ',' + mid_y + ', L ' + final_x + ','+final_y+' Z';
  //     return output_string;
  //    });